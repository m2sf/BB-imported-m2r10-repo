(* (C) 2009-2016 by B.Kowarsch & R.Sutcliffe. All rights reserved. *)

DEFINITION MODULE StdBitsets;

(* Alias types for Bitsets *) 


(* BITSET - Alias for bitset type of same bit width as CARDINAL *)

IMPORT BITSET+; (* import into client module *)


(* LONGBITSET - Alias for bitset type of same bit width as LONGCARD *)

IMPORT LONGBITSET+; (* import into client module *)


(* SHORTBITSET - Alias for smallest bitset type *)

IMPORT SHORTBITSET+; (* import into client module *)


(* LONGLONGBITSET - Alias for largest bitset type *)

IMPORT LONGLONGBITSET+; (* import into client module *)


IMPORT BS16, BS32, BS64, BS128;


(* BITSET16 - Alias type for 16-bit bitset type *)

TYPE BITSET16 = ALIAS OF BS16;


(* BITSET32 - Alias type for 32-bit bitset type *)

TYPE BITSET32 = ALIAS OF BS32;


(* BITSET64 - Alias type for 64-bit bitset type *)

TYPE BITSET64 = ALIAS OF BS64;


(* BITSET128 - Alias type for 128-bit bitset type *)

TYPE BITSET128 = ALIAS OF BS128;


END StdBitsets.